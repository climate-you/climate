# Generated by scripts/make_*_story_data.py, deliberately minified: it ships in
# the client bundle, and pretty-printing it costs ~40% more bytes. The generator
# would also rewrite it on the next run, so formatting it here only flip-flops.
src/content/stories/*/data.json
