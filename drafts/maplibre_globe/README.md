# MapLibre Globe Clone

This project reproduces the look and interactions from `mapTiler_globe`, but uses open-source `maplibre-gl` and a free public style.

## Run

```bash
cd maplibre_globe
npm install
npm run dev
```

## Notes

- No API key required.
- Base style source: `https://demotiles.maplibre.org/style.json`.
